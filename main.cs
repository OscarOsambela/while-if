using System;

class MainClass {
  public static void Main (string[] args) {
    Random numero = new Random();
    int aleatorio = numero.Next(0, 20);
    int intentos = 0;
    Console.WriteLine ("Ingresa un número entre el 0 y el 20");
    int minumero = int.Parse(Console.ReadLine());
    while(numero !=)
  }
}